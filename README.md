Clone the project using `git clone https://github.com/PrashamMehta/Tour_And_Travel.git`

Navigate to the project:
`cd .\Tour_And_Travel\`

Install the packages:
`npm run install`

Start the project on localhost:
`npm run start`
